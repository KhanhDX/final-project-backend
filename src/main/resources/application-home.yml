spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/final_project?autoReconnect=true&useSSL=false
    username: root
    password: Khanhkipkip1998
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

app:
  jwtSecret: KhanhDX
  jwtExpirationInMs: 604800000